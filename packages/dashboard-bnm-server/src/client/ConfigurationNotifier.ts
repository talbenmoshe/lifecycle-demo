import { /*  ConfigValue,  */IConfigurationNotifier } from './ConfigServerFacade';
// import axios from 'axios';

export class ConfigurationNotifier implements IConfigurationNotifier {
  async notify(/* artifactId: string, key: string, value: ConfigValue */): Promise<void> {
    // TODO
    // axios.post('http://localhost:3000/notify', {
  }
}